//! Funding-provenance measurement over real on-chain data: partition an anonymity
//! set by where each member's capital came from and report effective-k rather than
//! nominal k. This is a measurement crate; it makes no privacy claims by itself.
#![forbid(unsafe_code)]
